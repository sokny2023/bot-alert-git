version: '3.8'
services:
  git-alert:
    container_name: 'alert-to-bot'
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    environment:
      - TZ=Asia/Bangkok
    ports:
      - '5555:2024'
